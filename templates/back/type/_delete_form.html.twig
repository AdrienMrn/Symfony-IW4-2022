<form method="post" action="{{ path('back_app_type_delete', {'id': type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="btn">Delete</button>
</form>

{% embed 'back/_embed/_modal.html.twig' %}
    {% block header %}test{% endblock %}
    {% block main %}testmain{% endblock %}
{% endembed %}
